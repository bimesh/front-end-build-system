'use strict';

describe('Controller: AboutCtrl', function () {

  // load the controller's module
  beforeEach(function() {

  });

  it('should have a global object name Automation', function () {
    expect(Automation).not.toBeUndefined();
  });
});
